// this is where i'll build the model and animation for which current player is selected
import React from "react";

export default function PlayerModel() {
  return (
    <div>
      <i class="fa-solid fa-user-astronaut">hello</i>
    </div>
  );
}
